git init   #init
git add README  #添加文件
git commit -m 'first commit' # 提交文件
git remote add origin https://github.com/username/Hello-World.git   #指向我的github,本地的叫origin
git push origin master # 提交到master
git pull origin master # update